---
title: "Person Services"
description: "Find and enrich LinkedIn profiles and contact information"
icon: "user"
---

## Overview

Person services provide comprehensive tools for discovering and enriching individual profiles, primarily focused on LinkedIn data and contact information discovery.

## Available Services

<CardGroup cols={2}>
   <Card title="LinkedIn Services" icon="linkedin" href="/services/person/linkedin">
      Find and enrich LinkedIn profiles with detailed information
   </Card>
   <Card title="Contact Services" icon="address-book" href="/services/person/contact">
      Get verified email addresses and phone numbers
   </Card>
</CardGroup>

## Common Use Cases

### Finding a Prospect's LinkedIn Profile

```typescript
// Find LinkedIn URL for prospects in your list
const name = ctx.thisRow.get("Name");
const company = ctx.thisRow.get("Company");
const title = ctx.thisRow.get("Title");

const url = await services.person.linkedin.findUrl({
   name,
   company,
   title,
});

return url;
```

### Enriching Prospect Data

```typescript
const linkedinUrl = ctx.thisRow.get("LinkedIn URL");

const profile = await services.person.linkedin.enrich({
   url: linkedinUrl,
});

return profile.job_title;
```

### Getting Prospect Contact Information

```typescript
const linkedinUrl = ctx.thisRow.get("LinkedIn URL");

const contact = await services.person.contact.get({
   linkedinUrl,
   required: ["email"],
});

// Return work email (best for B2B outreach)
if (contact.work_emails && contact.work_emails.length > 0) {
   return contact.work_emails[0];
} else if (contact.personal_emails && contact.personal_emails.length > 0) {
   return contact.personal_emails[0];
}
```

## Data Quality

<Tip>
   All person data is regularly updated and validated. LinkedIn profiles include timestamps indicating when data was
   last refreshed.
</Tip>

<Warning>
   Always respect privacy laws and regulations when using contact information. Ensure you have proper consent and legal
   basis for contacting individuals.
</Warning>
